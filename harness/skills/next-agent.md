# Skill `/next-agent`

Próximo agente neste repo: [`docs/AGENTE-PROXIMO.md`](../../docs/AGENTE-PROXIMO.md).

1. Ramo de trabalho: **`main`** (pós-merge) ou `arena/<nova-sessao>` a partir de `main`.
2. Missão 1: completar `examples/01`–`05` se o esqueleto não chegar.
3. Missão 2: testes (`examples/examples.test.mjs` + `src/**/*.test.ts`).
4. Missão 3: checklist de docs em AGENTE-PROXIMO.
5. Proibido: Docker como CPU, checkout de `actos/fs`, apagar `arena/01a01e33-github-actions`.
