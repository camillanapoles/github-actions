name: ci

on:
  workflow_dispatch:
  pull_request:

# Least privilege — codebots/actionlint exigem `permissions` explícitas.
permissions:
  contents: read

jobs:
  qa:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      NODE_NO_WARNINGS: "1"
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || 'arena/01a01e33-github-actions' }}
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm test
