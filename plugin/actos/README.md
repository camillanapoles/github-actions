# Plugin ACTOS

Instala noutro repo. O runner passa a ser o CPU. Docker deixa de executar o trabalho.

```bash
bash plugin/actos/install.sh /path/to/alvo
```

Docs: [`docs/CONVERTER.md`](../../docs/CONVERTER.md) · skill: [`harness/skills/convert.md`](../../harness/skills/convert.md)

Receita incluída: `llm-infra-planner` → `actos-llmcalc.yml`.
