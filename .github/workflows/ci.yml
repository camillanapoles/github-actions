name: ci

on:
  workflow_dispatch:
  pull_request:

# Least privilege — codebots/actionlint exigem `permissions` explícitas.
permissions:
  contents: read

jobs:
  qa:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      NODE_NO_WARNINGS: "1"
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || 'arena/01a01e33-github-actions' }}
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm test
